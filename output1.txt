{
  "lifecycle": {
    "functions": [
      {
        "name": "aboutToAppear",
        "scope": "component",
        "description": "组件即将出现时触发，用于初始化操作"
      },
      {
        "name": "onDidBuild",
        "scope": "component",
        "description": "组件完成构建后触发，用于构建后的处理"
      },
      {
        "name": "aboutToDisappear",
        "scope": "component",
        "description": "组件即将消失时触发，用于清理资源"
      }
    ],
    "order": [
      "Parent.aboutToAppear → Parent.build → Parent.onDidBuild → Child.aboutToAppear → Child.build → Child.onDidBuild",
      "组件消失时：Parent.aboutToDisappear → Child.aboutToDisappear"
    ],
    "dynamicBehavior": "当条件渲染状态改变时（如showChild从true变为false），子组件会触发aboutToDisappear；当重新显示时（showChild从false变为true），子组件会按aboutToAppear → build → onDidBuild顺序重新初始化。父组件的生命周期不受子组件动态变化影响，仅在整个页面销毁时触发aboutToDisappear。"
  }
}